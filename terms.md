Adding one independent variable reduced the MSE by 0.49.

This reduction in MSE is called _coefficient of determination_ ($`R^2`$).

The coefficient of determination ($`R^2`$) of a model is equal to
```math
1 - (Model MSE) / (Base model MSE)
```
